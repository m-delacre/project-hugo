{% if app.user == false %}
    <nav class="navbar navbar-expand-lg px-0 py-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="/images/logo.png" alt="phugo_logo" width="35" height="40">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="text-dark nav-link {{ app.current_route == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="{{ app.current_route == 'app_home' ? 'nav-active' : '' }}" href="{{ path('app_home') }}">A propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="{{ app.current_route == 'app_home2' ? 'nav-active' : '' }}" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                     <li class="nav-item">
                        <a class="{{ app.current_route == 'app_login' ? 'nav-active' : '' }}" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% else %}
    <nav class="navbar navbar-expand-lg px-0 py-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="/images/logo.png" alt="phugo_logo" width="35" height="40">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="text-dark nav-link {{ app.current_route == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="text-dark nav-link {{ app.current_route == 'app_bibliotheque' ? 'active' : '' }}" href="{{ path('app_bibliotheque') }}">Bibliothèque</a>
                    </li>
                    <li class="nav-item">
                        <a class="text-dark nav-link {{ app.current_route == 'app_book_create' ? 'active' : '' }}" href="{{ path('app_book_create') }}">Ajouter un livre</a>
                    </li>
                    <li class="nav-item">
                        <a class="text-dark nav-link" href="#">{{ app.user.username }}</a>
                    </li>
                     <li class="nav-item">
                        <a class="text-dark nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endif %}

